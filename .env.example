# These values go into Vercel (Project Settings -> Environment Variables),
# NOT into the code and NOT committed to GitHub. Copy this file to .env.local
# with the real values only if you ever run the dashboard locally.

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=
GEMINI_API_KEY=
